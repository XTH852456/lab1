#2 月 12 日（周四）：基础核心 ——Ch2 批处理系统 + Ch3 多道程序与分时多任务
第二章 批处理系统（紧扣文档核心内容）

    理论吃透：特权级机制（RISC-V 特权级架构 / 特权指令）、应用程序设计（内存布局 / 项目结构）、RISC-V 特权级切换（控制状态寄存器 / 硬件机制 / 用户栈 & 内核栈）、Trap 管理（上下文保存恢复 / 分发处理）、系统调用基础实现；
    代码实操：① 按文档步骤编译生成应用程序二进制码，实现裸机运行应用；② 把应用程序链接到内核，完成特权级切换代码调试；③ 实现基础 sys_write 功能，完成文档指定的sys_write 安全检查实践作业；
    复盘：梳理文档中本章的代码树结构，画 Trap 处理 + 特权级切换的核心执行流程。

第三章 多道程序与分时多任务

    理论吃透：多道程序运行的核心原理、分时多任务的时间片调度基础、多任务下的上下文切换设计、与 Ch2 单任务批处理系统的核心区别；
    代码实操：① 基于文档代码导读实现多任务的基础调度框架；② 调试多程序的切换流程，验证分时调度的效果；③ 结合 Ch2 的 Trap 机制，适配多任务下的中断与系统调用处理；
    练习：完成章节基础问答题，理解多任务下内核资源的基础管理逻辑。

#2 月 13 日（周五）：地址与进程 ——Ch4 地址空间 + Ch5 进程
第四章 地址空间

    理论吃透：地址空间的核心设计逻辑、RISC-V SV39 分页机制（文档核心架构）、虚实地址转换、内核 / 用户地址空间的隔离设计、页表的创建与映射原理；
    代码实操：① 按文档代码树实现页表的核心数据结构；② 完成内核地址空间的映射调试，验证虚实地址转换的正确性；③ 为用户程序分配独立地址空间，实现地址空间的切换；
    重点：掌握文档中地址空间与前序章节（多任务、特权级）的联动实现细节。

第五章 进程

    理论吃透：进程控制块（PCB）的核心设计、进程的状态转换（运行 / 就绪 / 阻塞）、进程的创建 / 执行 / 退出（fork/exec/exit）原理、进程调度队列的实现、调度算法的基础应用；
    代码实操：① 定义 Rust 版 PCB 结构体，按文档步骤完成进程初始化与创建；② 搭建调度队列，实现基础分时调度算法；③ 调试 fork 创建子进程、进程退出与资源释放的全流程；
    练习：完成章节编程题，改造调度算法实现简单的优先级调度。

#2 月 14 日（周六）：文件与通信 ——Ch6 文件系统 + Ch7 进程间通信与 I/O 重定向
第六章 文件系统

    理论吃透：文件系统的核心抽象（文件 / 目录）、inode 基础、简易文件系统的整体架构、文件操作（open/read/write/close）的内核实现逻辑、文件系统与存储的基础交互；
    代码实操：① 按文档实现文件系统的核心数据结构（inode、目录项、文件描述符）；② 完成 open/read/write/close 系统调用的实现，调试用户态对文件的基本操作；③ 实现文件系统的基础挂载与初始化；
    重点：掌握文档中文件系统与地址空间、进程的联动，理解文件描述符与进程的绑定关系。

第七章 进程间通信与 I/O 重定向

    理论吃透：进程间通信（IPC）的基础方式、管道通信的核心原理、标准输入 / 输出 / 错误的重定向机制、I/O 重定向与文件系统的联动逻辑；
    代码实操：① 实现匿名管道的核心功能，调试两个进程间的管道通信；② 完成标准输出重定向（>）、输入重定向（<）的代码实现；③ 结合管道与重定向，实现多进程的通信与数据传递；
    练习：完成章节实验，实现管道的嵌套使用与重定向的组合操作。

#2 月 15 日（周日）：并发安全 ——Ch8 并发

    理论吃透：文档中并发的核心问题（临界区）、忙等与睡眠等待的区别、自旋锁 / 互斥锁的实现原理、死锁的产生与避免方法、内核中并发的典型应用场景（调度队列 / 共享文件 / 外设资源）；
    代码实操：① 用 Rust 原子操作实现自旋锁与互斥锁，保证锁的安全性；② 为前序章节的共享资源（PCB 调度队列、文件描述符表、共享内存）添加锁保护；③ 调试多进程并发访问共享资源的场景，解决数据竞争问题；
    重点：掌握文档中 Rust 特性在内核并发中的应用，理解无锁编程与锁机制的适用场景；
    练习：完成章节编程题，实现生产者 - 消费者模型（基于条件变量 / 锁）。

#2 月 16 日（周一）：设备管理 + Ch2~Ch9 全章节复盘（紧扣 Ch9 文档核心）
第九章 I/O 设备管理（严格贴合文档内容）

    理论吃透：① I/O 核心：设备分类、I/O 传输方式（PIO / 中断 IO/DMA）、5 种 I/O 执行模型（阻塞 / 非阻塞 / 多路复用 / 信号驱动 / 异步）及对比；② 设备抽象：基于文件 / 流 /virtio 的 I/O 设备抽象、设备树（信息传递 / 解析）；③ 具体驱动：串口驱动（初始化 / 输入输出）、virtio 设备架构（virtqueue 虚拟队列 / MMIO 交互）、virtio-blk 块设备 /virtio-gpu 显卡设备的核心数据结构与操作逻辑；
    代码实操：① 解析设备树获取外设信息，实现文档中的基础串口驱动并调试；② 完成 virtio 设备的初始化，调试 virtqueue 的创建与管理；③ 实现 virtio-blk 块设备的基础读 / 写操作，可选：移植embedded-graphics库实现贪吃蛇图形化应用；
    实验：完成文档指定的支持图形显示的应用实践作业，按要求提交实验报告。

Ch2~Ch9 全章节整合复盘（贴合文档整体逻辑）

    流程串讲：按文档技术脉络，梳理批处理单任务→多道分时任务→地址空间隔离→进程管理→文件系统→进程通信→并发安全→I/O 设备管理的完整内核实现链路，明确每个章节的核心作用与联动点；
    代码整合：将各章节核心代码模块（Trap / 特权级、上下文切换、页表、进程调度、文件系统、管道、锁机制、设备驱动）整合为完整简易内核，在 QEMU 中编译运行验证；
    练习收尾：完成 Ch2~Ch9 所有文档指定的课后练习（编程题 + 问答题），标记薄弱点（如 virtio 驱动、分页机制）并结合文档补充学习；
    最终验证：能口述从用户应用启动→触发系统调用→进程调度→地址转换→文件 / 通信操作→并发访问→设备 I/O的全流程，覆盖文档所有核心知识点。
    该文档使用了豆包AIagent的辅助
